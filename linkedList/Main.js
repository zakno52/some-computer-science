import { LinkedList } from './LinkedList.js';

const list = new LinkedList();

// Test your methods here (example):
list.append('dog');
list.append('cat');
console.log(list.toString()); // Should print: ( dog ) -> ( cat ) -> null
